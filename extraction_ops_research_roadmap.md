# Extraction Ops 研究路线图

> 本文档定义 Extraction Ops × verl-agent 实验的核心科学问题与后续方向。
> 当前状态见 git 历史与 `docs/archive/` 中的历史状态文档；本文只写"往哪走、为什么、怎么走"。

---

## 0. 定位：我们到底在研究什么

**一句话**：一个神经网络（Qwen3-0.6B，LoRA）能否通过训练获得**图规划能力**，并在未见布局上泛化。

关键前提——**BFS 专家不是被研究对象，它是三样工具**：

| 角色 | 用途 |
|---|---|
| 可行性证明器 | 保证生成的每张图都有解（避免用死图训练） |
| 老师 | 产出 SFT/DAgger 的监督标签（正确动作轨迹） |
| 评测标尺 | 定义最优轨迹与 100% 成功上限，量化模型差距 |

被研究、被训练、被评测的是**模型**。BFS 是代码在跑图搜索，模型是 token 预测器在"读文字图 + 隐式规划 + 输出动作"。二者能力不等价，这正是实验存在的意义。

**单解问题只是起点**。如果模型只能复制 BFS 的唯一最短路径，那么：
- SFT 抄老师就够了，RL 没有存在价值；
- 实验无法回答"学到的到底是规划能力，还是对特定轨迹的死记硬背"。

因此必须向下面三个方向推进，否则实验确实单薄。

---

## 1. 现状总结（截至 Stage A）

已完成：
- 固定世界 → 8 模板 procedural → Stage A 有界随机图生成器；
- 固定世界 SFT 100%；程序化 SFT held-out 18~25%；20 步 GiGPO 退化到 0%；recovery SFT/DAgger 3-seed 100%；
- 随机图生成器 300/300 被 BFS 专家通关，布局互不相同。

关键代码位置：
- `agent_system/environments/env_package/extraction_ops/generator.py`：`DEFAULT_RANDOM_CONFIG`、`layout_id`、`structural_invariants`、`validate_definition`
- `world.py`：`random_maps` 模式、milestone 奖励
- `expert.py`：BFS 最短路径（tie-break 确定性排序，只输出**一条**轨迹）
- `examples/evaluation/check_extraction_ops_random_maps.py`：冒烟验证

当前奖励结构（`world.py` 的 `REWARD_SCHEMES`，默认 `milestone`，向后兼容）：
```
milestone（旧默认）:  key +0.05 / objective +0.15 / success +1.00 / premature +0.05 / invalid -0.02
outcome（B1 已实现）:  key +0.02 / objective +0.03 / success +1.00 / premature  0.00 / invalid -0.02
sparse（教学/消融）:   key  0.00 / objective  0.00 / success +1.00 / premature  0.00 / invalid -0.02
```

**尚未解决的核心问题**：
1. **SFT 管线未验证（当前最优先，见 §4.5）**：随机地图 SFT 后 train loss 0.017 但连训练 seeds 成功率仅 9.4%——0.6B 硬背 2076 步理应背得下来，说明训练侧存在未定位的问题（1 epoch 记忆不足 / 频率偏置 / exposure bias，三者皆非容量问题）；
2. RL 在稀疏/结果奖励下不稳定（20-step GiGPO 已翻车）；
3. 模型只会模仿老师的一条解，没有多样性；
4. 没有"无解"概念，模型对任何图都只会循环到超时。

**近期教训（2026-08-22 复盘）**：
- 随机地图 SFT 的 48% 提前撤离局全部拿到 +0.05/+0.10 正奖励——评估脚本未显式传 `reward_scheme`，落在默认 `milestone`（提前撤离 +0.05），这是评估侧的 reward trap，必须与训练侧对齐为 `outcome`；
- "低 SFT loss ≠ 学会任务"再次验证：loss 0.017 的模型 rollout 时学到的只是"冲撤离点"这一全局高频捷径，而非逐图路径。

---

## 2. 三个创新方向

### 方向 A：多解探索 —— 证明 RL 价值的唯一场景

**要回答的科学问题**：当任务存在多条等价合法解时，RL 能否让模型发现**老师没演示过的解**？SFT 是否必然塌缩到单一解？

这是"RL > 模仿学习"的决定性证据。SFT 只能抄老师分布，而 RL 用结果奖励驱动，理论上可以发现新解。

**依赖**：
- 必须先有**稳定的结果奖励训练**（目前没有，见方向 B1）。

**需要的代码改动**：

| 组件 | 改动 |
|---|---|
| `generator.py` | 多个撤离点、多把可互相替代的钥匙、多个等价目标容器、更多环边（复用现有 `extra_edges`） |
| `expert.py` | 从"唯一最短路径"改为**多样化解采样器**：随机 tie-break、随机 pace、随机起点、路径随机化，输出多条不同合法轨迹 |
| `world.py` | 奖励改为**结果导向**：成功 +1，失败 0，可选轻步数惩罚；不再按特定路径给 milestone 分 |
| 评测 | 新增**多样性指标**：跨采样/温度下模型能输出多少条 distinct 合法轨迹 |

**风险（高）**：
- 0.6B 探索能力弱，稀疏奖励可能不收敛（已有先例：GiGPO 打到 0%）；
- 很可能得到**负结果**："SFT 塌缩、RL 加不出多样性"。这个负结果本身有科学价值，但要有心理预期。

**验收标准**：
- 模型在 held-out 布局上，对同一任务能输出 ≥2 条不同合法解，且其中至少一条 ≠ 老师轨迹；
- 或（负结果）记录"多样性无提升"的完整证据与根因。

---

### 方向 B：解复杂度与长度泛化

**要回答的科学问题**：模型能否泛化到训练分布之外的**路径长度、钥匙数量、房间数量**？

**性质**：这是"同一技能的加压"，不是新能力。Transformers 的长度/组合泛化是已知硬骨头。

**依赖**：无，纯扩展 `generator.py` 参数即可起步。

**需要的代码改动**：

| 组件 | 改动 |
|---|---|
| `generator.py` | 参数化：更多房间、多把锁、干扰钥匙、多容器、带噪声/不完整 intel；按复杂度分层生成 |
| 评测 | 按"最短解路径长度 / 钥匙数 / 房间数"分桶，画 success 曲线，找能力边界 |
| 模型规模 | 上 1.7B 之前必须先通过 §4.5 B1.5 的 SFT 管线验证（0.6B 多 epoch 能背下训练图），否则 1.7B 同样学不会 |

**风险（中）**：0.6B 大概率在某长度崩掉，但**测量出边界本身就是结果**。

**验收标准**：得到 success-vs-复杂度曲线，明确当前模型的能力边界，并给出 1.7B 对照结论。

---

### 方向 C：无解检测与主动放弃

**要回答的科学问题**：模型能否学会"**判断任务无解并正确放弃**"，而不是死循环到超时？

这是真实 agent 最稀缺的能力之一（现有 agent 普遍只会无限重试）。

**性质**：任务性质的改变（从"找解"到"判定可解性"），与前两个方向正交，建议**独立立项**。

**依赖**：与 A/B 无强依赖。

**需要的代码改动**：

| 组件 | 改动 |
|---|---|
| `world.py` | 新增动作 `declare_impossible`；新增"无解图"生成模式（故意破坏 feasibility：钥匙被锁在目标门后、撤离点不可达等） |
| `expert.py` | BFS 天然能判无解（搜不到路径 = 无解），作为"可行性判定 oracle"输出 `abort` 标签（无轨迹标签） |
| 奖励 | 定义"正确放弃"的奖励：对无解图正确 abort = +1；对有解图误 abort = 重罚；死循环 = 0 |
| 评测 | 无解图正确放弃率（recall）、有解图误放弃率（false abort） |

**风险（高）**：
- reward 设计难：放弃过松会"瞎放弃"，过严会"死循环"；
- SFT 监督信号变弱（无解图没有轨迹标签，只有 abort 标签）。

**验收标准**：无解图正确 abort ≥ X%，有解图误放弃 ≤ Y%（X/Y 待定，需先小规模校准）。

---

## 3. 跨方向方法论：课程学习（Curriculum Learning）

**定位**：不是独立方向，而是 A/B/C 都能用的训练策略。**有效性待后续实验验证**——可能无增益甚至更差，必须与无课程的随机混合做对照。

**要回答的问题**：按难度从易到难喂训练数据，能否改善收敛稳定性与长度泛化？

**为什么对我们对症**：
- **B1（RL 稳定）**：稀疏奖励 + 难任务 = 双重困难。课程让 RL 先在简单地图上（成功率高、奖励信号密）站稳，再逐步加难度——这是对抗稀疏奖励的经典组合拳。
- **方向 B（长度泛化）**：Transformers"训练短、测试长"是已知硬骨头，课程学习是少数被验证有效的缓解手段之一。

**与现有实验的区别**：固定世界 → 8 模板 → 随机地图是**手动分阶段**（开发路径），不是训练内课程。真正的课程学习要求**同一次训练内**按难度排序喂数据、自动调度。

**我们已经具备的零件**：
- 难度旋钮：`generator.py` 的 `DEFAULT_RANDOM_CONFIG`（核心房间数、门数量、环边数）；
- 难度度量：**BFS 最短解长度**（`validate_definition` 可计算），跨布局可比，是最干净的难度指标。

**实现方案（分两步）**：
1. 简单版——固定难度带 schedule：按 BFS 最短解长度分桶（如 10~14 / 15~19 / 20~24），每 N step 升一档；
2. 进阶版——性能触发：当前带 held-out 成功率 ≥ X% 才升档，否则继续留在当前带。

**接入点**：
- SFT：数据采样时按难度分桶，先易后难；
- RL：rollout 时按当前难度带设置 `random_maps_config`。

**风险与未知**：
- 有效性未验证；过早升难度可能引发灾难性遗忘；
- 必须有"无课程随机混合"baseline 对照才能下结论。

**验收标准**：相同总训练量下，课程学习 vs 随机混合的 held-out 成功率 / 长度泛化曲线存在可复现差异。

---

## 4. 依赖关系与建议顺序

```
阶段 B1：结果导向奖励（保留弱 milestone 信号）          ✅ 已完成
  └─ 在现有单解随机图上先把 RL 跑稳  ← 这是 A 的地基

阶段 B1.5：SFT 管线验证（1.7B 的前置门槛）             ★ 当前所在
  └─ 证明 0.6B 能背下训练数据，再谈 scaling；
     若 SFT 管线有 bug，1.7B 同样学不会，先修管线

阶段 A1：多解生成器 + 多样化解采样
  └─ 先只测"任意一条 ≠ 老师 的解"，别追求多样性分布

阶段 B2：复杂度分层压测（长路径/多钥匙/噪声 intel）
  └─ 画能力边界曲线；1.7B 对照仅在 B1.5 通过后执行

阶段 C：无解检测（独立立项）
```

### 4.5 B1.5：SFT 管线验证（新增，1.7B 的前置门槛）

**背景**：随机地图 SFT（修复观察 bug 后）train loss 0.017、val loss 0.028，但三层验证 train_like 仅 9.4%（连训练 seeds 都学不会）。0.6B 硬背 2076 步（128 局 × ~16 步）理应背得下来——因此**这不是容量上限，而是训练侧问题**。

**三个候选根因（按嫌疑排序）**：
1. **1 epoch 记忆不足**：模型对每张图只见过一遍，0.6B 不足以一遍记住 128 张图的完整映射；
2. **频率偏置**：expert 数据中"move 撤离点→extract"每局结尾都出现（128 次），是最高频模式，模型学到"冲撤离点"捷径先验（48% 提前撤离局正是它）；
3. **exposure bias**：teacher forcing 下 loss 低 ≠ rollout 稳，第一步偏离后全部 OOD。

**验证实验（同数据同配置，只改变量）**：
1. 0.6B 重训 5~10 epoch（仅改 `EPOCHS`），跑三层验证：train_like 应大幅上升（记忆生效）；
2. teacher-forcing 回放诊断：把 expert 每步 obs 喂给合并模型，测 next-action 准确率——高但 rollout 差 = exposure bias；本身也低 = prompt/数据不一致 bug（需查 `generate_extraction_ops_expert.py` 的 prompt 构造 vs 评估侧 `apply_chat_template`）；
3. 评估脚本显式传 `reward_scheme="outcome"`（当前默认 milestone 给提前撤离 +0.05，污染指标）。

**判据与后续**：
- train_like ≥ 50%（多 epoch 后）→ 确认是记忆不足；再测 held-out（fixed/rolling）看是否泛化。泛化则 SFT 管线 OK，**此时才允许训 1.7B 对照**；只记忆不泛化则回到 DAgger/课程学习；
- train_like 仍 < 30% → SFT 管线有 bug，深挖数据生成，**1.7B 无限期顺延**。

理由：
1. **B1 最优先**：没有稳定的结果奖励，方向 A 无从谈起；这也是对上次 GiGPO 翻车的正面修复。
2. **A1 次之**：它是"RL 价值"的核心，但必须建立在 B1 之上。
3. **B2 可并行**：纯 generator 参数扩展，不依赖 RL，随时可做。
4. **C 最后且独立**：任务性质变了，避免和 A/B 混在一起。
5. **课程学习贯穿 B1/B2**（见 §3）：在简单难度带上先把 RL 站稳再升难度；有效性待实验验证。

---

## 5. 组件改动总览

| 方向 | generator.py | expert.py | world.py | 评测 | 训练配置 |
|---|---|---|---|---|---|
| B1 结果奖励 | — | — | 奖励结构 | 单解成功率 | 低 LR + 高 KL + SFT replay |
| A 多解 | 多撤离点/多钥匙/多容器/多环 | 多样化解采样器 | 结果奖励 | 多样性指标 | RL（结果奖励） |
| B 复杂度 | 参数化分层 | BFS 支持多锁 | — | 复杂度分桶曲线 | SFT + RL |
| C 无解 | 无解图模式 | 可行性判定 oracle | `declare_impossible` 动作 | abort recall/false-abort | SFT（abort 标签）+ RL |
| 课程学习（跨方向） | 难度带 config | BFS 最短解长度度量 | — | 分难度带曲线 | 固定 schedule / 性能触发 |

---

## 6. 风险总览

| 风险 | 方向 | 缓解 |
|---|---|---|
| **未验证 SFT 管线就上 1.7B（浪费算力且误导结论）** | B1.5 | 0.6B 多 epoch 先证明能背下训练图；teacher-forcing 回放排除 prompt 不一致；评估 reward_scheme 与训练对齐 |
| 稀疏奖励不收敛（已有先例） | A/B1 | 保留弱 milestone 信号、SFT warm-start、低 LR、高 KL、SFT replay |
| 0.6B 能力上限 | A/B | 及时上 1.7B 对照，不无限调参 |
| 负结果 | A | 接受并完整记录（"SFT 塌缩、RL 无多样性"也是结论） |
| 无解 reward 设计翻车 | C | 先小规模校准 X/Y 阈值，再定验收 |
| 生成器破坏可行性 | A/B | `structural_invariants` + BFS 全量复验（已有 `validate_definition`） |
| 课程学习无效或有害 | 跨方向 | 与无课程随机混合对照；性能触发门槛防过早升难度 |

---

## 7. 结论

这个实验的价值不在"跑通一个玩具游戏"，而在回答一组可证伪的问题：

1. 模型能否学会规划（不只是背轨迹）——靠 held-out 布局 + 复杂度曲线；
2. RL 能否超越模仿——靠多解探索；
3. 模型能否学会"知难而退"——靠无解检测。

BFS 永远是那把尺子，但**尺子量的是什么**，由上面三个方向决定。

**当前闸门（2026-08-22 更新）**：任何 scaling（1.7B）实验之前，必须先通过 B1.5 的 SFT 管线验证——"模型背不下训练数据"时优先怀疑训练管线，而不是模型容量（观察 bug 已证明过一次这个教训，train_like 9.4% 是第二次）。
